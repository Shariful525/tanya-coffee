export interface IProject {
  clientLogos: string[]; // array of image URLs
  client: string;
  campaign: string;
  ourWork: string[]; // array of tasks/works
  result: string[]; // array of results
  projectImages: string[];
}
